---

  name: "setupjmetertestchain"
  description: "Setup the jmeter test file to run against instance"

  runner_type: "action-chain"

  entry_point: "workflows/setupjmetertestchain.yaml"

  enabled: true
  parameters:
    app_ami_name:
      type: "string"
      required: true
    pipeline_id:
      type: "string"
      required: true
    jmeter_file:
      type: "string"
      required: true
    
